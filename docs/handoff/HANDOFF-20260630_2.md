# Handoff — 2026-06-30 (2차)

> 이전 핸드오프: `HANDOFF-20260630.md` — Round 22-Privacy 배포 완료
> 이번 핸드오프: **Round 22 본작업(지도/업데이트 SEO + 내부 링크) 구현 → 커밋 → 푸시 → 라이브 배포 완료**

## 프로젝트 현재 상태 한 줄

**Round 22 단독 배포 완료. 라이브 §8 회귀검증 전 항목 PASS. 다음 후보는 Round 24 AdSense 사전 감사(Round 22와 혼합 금지).**

---

## Round 22 목적과 최종 결정

목적: 지도 페이지를 "서울 심야버스 노선·환승" 대표 페이지로, 업데이트 글을 "공개·이용 방법" 지원 문서로 **역할 분리**하고 내부 링크 신호를 보강.

확정 결정 5건:
1. 업데이트 `title ≠ H1` 분리 → `UpdateEntry.h1Ko?` 선택 필드 신설
2. `dateModified` 분리 → `UpdateEntry.dateModified?` 선택 필드 신설
3. 기능 안내 중복 제거 → 페이지에 신규 안내 추가 + `NightBusMap.tsx` 내부 안내(`선은 직접 작도…`) 삭제
4. 지도 H2 본문 1문단 확정 교체 + 정확성 고지 박스 SVG 뒤로 이동·축약
5. 홈 지도 CTA는 **서버 컴포넌트 locale 게이트**(클라이언트 `useLocale()` 미사용) → 초기 HTML에 `<a>` 노출
- 추가: 업데이트 글 하단 CTA 1개(FAQ 뒤, footer 직전) — 상·하단 총 2개

## 커밋 / 배포

- 구현 커밋: **`c5b8abc`** "Round 22: refine night bus map SEO and internal links" (11개 파일, +140/−35)
- 라이브 배포 완료일: **2026-06-30**
- origin/main · 서버 라이브 HEAD 모두 **`c5b8abc`**
- 새 이미지 ID: **`sha256:47a89e95...`**
- 롤백 태그 보존: **`seoul-autonomous-web:rollback-0a27b2c`** (= `29271d937402`, Privacy 빌드)

## 최종 라이브 URL / 메타

- 지도: https://seoulautonomous.com/ko/night-bus-map
  - title `서울 심야버스 노선과 N버스 환승 노선도`
  - H1 `서울 심야버스 노선과 환승 노선도`
  - meta `서울 심야버스 노선과 N버스 환승 경로를 한눈에 확인하세요. 올빼미버스 14개 노선과 심야A21의 주요 경유지와 환승역을 보고 출발역과 도착역을 선택해 직통 또는 환승 경로를 찾을 수 있습니다.`
- 업데이트: https://seoulautonomous.com/ko/updates/night-bus-map-launch
  - title `서울 올빼미버스 환승 지도 공개와 이용 방법`
  - H1 `서울 올빼미버스 환승 지도 공개`

## JSON-LD / 구조 상태

- 지도: **WebPage 1개**(name=H1, url, inLanguage:ko, isPartOf WebSite "Seoul Autonomous") + **BreadcrumbList**(홈/지도 2단계)
- 업데이트: **Article**(headline=title, description=summaryKo, datePublished `2026-06-14`, dateModified `2026-06-30`) + **FAQPage 8문항**(무변경) + **BreadcrumbList**
- 지도 DOM 순서: 기능안내 → 지도 → H2 → 정확성 고지 (확인됨)

## 내부 링크

- 홈 ko: 지도 CTA 카드 1개(서버 렌더), `/en` 홈에는 없음
- how-to-ride ko: "정류장 찾는 방법" 섹션 뒤 문맥 링크 1개, `/en/how-to-ride`에는 없음
- 업데이트: 상단 CTA + FAQ 뒤 하단 CTA = 2개, 둘 다 `/ko/night-bus-map`

## 404 / 검증

- `/en/night-bus-map`, `/en/updates/night-bus-map-launch` **하드 404 유지**
- 빌드 / 로컬 회귀검증 / 라이브 §8(A~E) **전 항목 PASS**
- 지도 인터랙션(노선칩·검색·환승1/2·크게보기·PNG·reset·핀치줌)은 **사용자 로컬 수동 확인 완료**(브라우저 확장 미연결로 자동화 생략)
- Caddy **미변경** — 기존 사이트(newsforgreens.com·fazr.co.kr) 200 유지
- 서버 로그 error·exception·hydration **0건**

## ⚠ 운영 주의사항 (반드시 기억)

- **서버 저장소에 추적 밖 `Dockerfile`(untracked)이 존재**하며, 현재 수동 Docker 빌드(`docker build .`)가 이 파일에 의존한다. `git pull --ff-only`는 tracked `web/`만 갱신하므로 이 Dockerfile과 충돌하지 않음. (저장소에는 Dockerfile이 커밋돼 있지 않음)
- **롤백 절차**: 배포는 `:latest` 태그를 덮으므로, 새 컨테이너 실패 시 `seoul-autonomous-web:rollback-0a27b2c`로 즉시 복귀 가능. 다음 배포 전에 동일 방식으로 현재 이미지를 박제할 것.
- 로컬 untracked 3건(`docs/handoff/HANDOFF-20260608.md`, `round19-final-이식지시서.md`, `route/`)은 **계속 미변경·커밋 제외**.

## 다음 작업

- **배포 후 관찰 기간**: title·H1·본문은 최소 2~4주 재수정하지 않고 GSC·네이버 검색 결과를 관찰한다.
- **다음 후보: Round 24 AdSense 사전 감사** (GA4 광고 설정·Consent·Privacy 광고 문구 등). **Round 22와 혼합하지 않는다.**
- Round 23(GA4 커스텀 이벤트)도 별도 라운드로 분리.
