# Handoff — 2026-06-30 (3차)

> 이전 핸드오프: `HANDOFF-20260630_2.md` — Round 22 본작업 배포 완료
> 이번 핸드오프: **Round 24-A(Privacy 광고 고지 + Terms 정정 + design-preview 프로덕션 제외) 구현 → 커밋 → 푸시 → 라이브 배포 완료**

## 프로젝트 현재 상태 한 줄

**Round 24-A 단독 배포 완료. 빌드·로컬·라이브 검증 전 항목 PASS. 다음은 R24-B 사전 감사(ads.txt + AdSense 사이트 연결 스크립트). 광고 슬롯·CMP·Consent Mode는 아직 미구현.**

---

## Round 24-A 목적

AdSense 신청 준비를 위한 법적 고지·테스트 페이지 정리(좁은 범위). 광고 코드 자체는 넣지 않음(그건 R24-B 이후).

1. **Privacy 광고 고지 신설** — Google Publisher Policies(제3자 광고 쿠키·웹비콘·IP 공개 요건) 대응
2. **Terms 정정** — "앱" 프레이밍 → "웹사이트", 구 운영정보(이메일·운영자명) 정합
3. **design-preview 프로덕션 제외** — 심사·색인 노출 차단

## 커밋 / 배포

- 구현 커밋: **`be8d412`** "Round 24-A: update privacy terms and hide design preview" (6개 파일, +85/−47)
- 라이브 배포일: **2026-06-30**
- origin/main · 서버 라이브 HEAD · 라이브 컨테이너 모두 **`be8d412`**
- 새 이미지 ID: **`5b647441350e`** (manifest `sha256:5b647441…`)
- 롤백 태그 박제: **`seoul-autonomous-web:rollback-c5b8abc`** (= `47a89e955939`, Round 22 빌드)
- 기존 롤백 태그 **`rollback-0a27b2c`**(`29271d937402`) 보존

## 변경 내용 (6개 파일)

- `web/data/legal/privacy.ko.ts` / `privacy.en.ts` — §9(제3자·국외 처리) 바로 뒤에 **§10 광고 / §10 Advertising** 신설, 이후 §11~§15 재번호. 조건부 표현("표시될 수 있습니다 / may"), 옵트아웃은 평문(렌더러 링크 미지원). 포함 의미: 제3자(Google) 광고·제3자 쿠키 설정/판독·웹비콘/IP·맞춤/비맞춤 광고·광고 개인최적화 관리·EEA/영국/스위스 지역 동의 메시지 가능성.
- `web/data/legal/terms.ko.ts` / `terms.en.ts` — 앱/the App/information app → 웹사이트/the Website, 다운로드·설치·사용 → 접속·이용, 이메일 `support@fazr.co.kr` → `seoulautonomous@protonmail.com`, 운영자 `fazr` → `Seoul Autonomous` + `developerLabel`(운영자/Operator), `lastUpdated` → `2026년 6월 30일` / `June 30, 2026`(시행일은 유지). 외부 예약앱 의미의 "별도 앱 / reservations, apps"는 의도적으로 유지. terms.ko는 기존 `\u` 이스케이프 인코딩 유지(수정 문장만 최소 diff).
- `web/app/design-preview/layout.tsx` — 서버 컴포넌트에 `if (process.env.NODE_ENV === 'production') notFound();` 추가 → **production 하드 404, dev 200**.
- `web/public/robots.txt` — `Disallow: /design-preview` **제거**(봇이 실제 404 확인 가능하게). Allow·Sitemap·Daum 인증줄 유지.

## 검증 결과 (전 항목 PASS)

- **빌드**: `npm run build` PASS (Next 16.2.4, TypeScript 통과, 57/57 정적 생성, 에러 0)
- **로컬**(prod build): /design-preview 404·dev 200, Privacy/Terms 렌더 정상
- **라이브**:
  - `/ko·en/privacy` 200 — §10 광고/Advertising 존재, §1~§15 연속, 조건부 표현, GA4·쿠키·권리·연락처 유지, publisher ID·광고코드 노출 0
  - `/ko·en/terms` 200 — 본 웹사이트/the Website, protonmail, 운영자/Operator, 수정일 2026-06-30, 별도 앱/apps 유지, 구표현·fazr 0건
  - `/design-preview` **하드 404**(Location 없음, soft 아님, "could not be found" 본문)
  - `/robots.txt` 200 — design-preview Disallow 제거, 기존 내용 유지
  - sitemap design-preview 0건
  - 루트 언어협상 정상(no-AL→/en, ko 브라우저→/ko), /ko·/en·/routes·/ko/night-bus-map 200
  - **Round 22 title·H1·meta·sitemap 구조 무변경**
  - 서버 로그 error·exception·hydration **0건**
- **다른 사이트 헬스체크**: newsforgreens.com 200, fazr.co.kr 200
- **Caddy 완전 미접촉** — 이번 배포에서 validate·reload·restart 일절 없음(코드/문구/robots만 변경 → 컨테이너 교체만)

## ⚠ 운영 주의사항 (반드시 기억)

- **서버 저장소에 추적 밖 `Dockerfile`(untracked) 존재** — 수동 Docker 빌드가 이 파일에 의존. `git pull --ff-only`는 tracked만 갱신(충돌 없음). 저장소엔 Dockerfile 미커밋.
- **롤백 절차**: 배포는 `:latest`를 덮음 → 다음 배포 전 현재 이미지를 `rollback-<해시>`로 박제할 것. 현재 직전 = `rollback-c5b8abc`, 그 전 = `rollback-0a27b2c`.
- 로컬 untracked 3건(`docs/handoff/HANDOFF-20260608.md`, `round19-final-이식지시서.md`, `route/`)은 계속 미변경·커밋 제외.

## 다음 작업 — R24-B (미착수)

- **R24-B 예정 범위: ads.txt + AdSense 사이트 연결 스크립트(`<head>` 삽입)**. 사용자 제공 publisher ID는 확인됨(보고서엔 `<CONFIRMED_PUBLISHER_ID>`로만 표기, 실값 노출 금지).
- 연결 스크립트 ≠ 광고 슬롯 — 연결 스크립트 삽입만으로 슬롯 생성 금지. 전역 layout 무조건 삽입 vs locale/design-preview/404 제외는 코드 구조 기준 설계 필요.
- **AdSense 광고 슬롯·자동광고·CMP·Consent Mode는 아직 미구현**(R24-C/D).
- 진행은 사전 감사부터 — Round 22/R24-A와 혼합 금지, 라운드 단독 배포·검증.
