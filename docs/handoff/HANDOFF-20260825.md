# HANDOFF 2026-08-25 — AdSense 3차 거절 · Phase 0 CTG 구조 감사 · Phase 1 방향 확정

> 이전 정본(HANDOFF-20260811 이하)은 소급 수정하지 않는다.
> 이번 세션 감사 정본: `docs/worklogs/PHASE0-CTG-STRUCTURE-AUDIT-20260825.md`

## A. 이번 세션 요약

```
성격         AdSense 3차 거절 확인 → Connected Transit Graph 전환 Phase 0
             read-only 구조 감사 → 포그린 검수 → Phase 1 방향 확정 → docs 정본화
판정         Phase 0 = EXIT 0 / BLOCK 없음 / APPROVED·CLOSED
코드 변경     0  (서버 접속 0, Docker/Caddy 0, 데이터 0)
docs 변경     이번 정본화 3건 (worklog 정본 · 이 핸드오프 · SESSION-HANDOFF)
```

## B. AdSense 3차 거절

- 2026-08-11 3차 재신청 → **거절**. 포그린이 AdSense 화면에서 직접 확인(2026-08-25 세션 전달):
  "정책 위반이 발견되었습니다 → **가치가 별로 없는 콘텐츠**"
  (+ "광고 게재가 준비되지 않은 사이트", "Google 게시자 네트워크의 사용 기준을 충족하지
  않고 있습니다" 안내).
- 이로써 "심사 대기 중 착수 금지" 잠금은 종료. 대응 전략: 꼼수·증량이 아니라
  **사이트 실질 가치를 높이는 구조 전환(CTG)**. 거절 원인이 그래프 부재로 확정된 것은
  아니다(미증명 — 과장 금지).
- 이력: 1차 거절 2026-07-10 / 2차 2026-07-29 / Round 26 수리 배포 2026-08-03 /
  Final Gate APPLY-NOW·3차 신청 2026-08-11 / **3차 거절 → 이번 세션**.
- 재신청은 구조 전환·색인 반영 후 별도 판단. **당분간 재신청 금지.**

## C. Phase 0 핵심 실측 (상세는 worklog 정본)

```
web Graph SSOT      web/data/routes.json (루트 data/routes.json은 RN 레거시, 비접촉)
Route               11 (+robotaxi 1은 MobilityService 후보, +pending 3 제외)
ARS Stop            267  (null 0 / 충돌 0 / 공식EN 261 / fallback 6 / 좌표 0)
StopVisit/raw       307  (동일 노선 내 stopId 반복 2건: 02247, 13156)
다노선 공유 Stop     33   (2노선 28 + 3노선 5) — A160·A148·A741·A21 회랑
동명 방향쌍          90   (전부 같은 노선 왕복쌍) → 물리 그룹 후보 177
노선 상세 페이지     리프 문서 (전역 내비·푸터 외 내부 링크 0)
로보택시            현재성 오류 0. 8/19 공식 발표(19대·평일 22~05시) 독립 검증 완료,
                    요금 4구간은 기존 게재값과 동일(불변). 반영은 별도 라운드
allnosun.com        전국 프로그래매틱(서울 2만 URL, AdSense 게재 중). 배울 것 =
                    노선↔정류장 양방향 실링크 그래프. 복제 금지 = 무출처 전량 페이지화
```

## D. 확정된 설계 결정 (포그린)

1. Phase 0 승인·종결. **STATIC-FIRST / NO EXTERNAL STATE NEEDED** (Upstash·Redis·KV·DB
   현재 불필요 — 도입 trigger 문서화됨)
2. 엔터티 3층: **Route → StopVisit → Stop**. 267 ARS = 원자 identity,
   177 물리 그룹 = 보조 계층 후보(URL 결정은 Phase 1B 후로 유보)
3. **267 Stop 전량 페이지 생성 금지** (특히 단일 노선 234개 기계 생성 금지)
4. 신규 dependency 0 · routes.json 원본 무수정(파생 레이어만) · C2E 영문명 계약 승계
5. 제품 목표 잠금: "서울에서 지금 어디서 무엇을 타야 하는지, 공식 근거와 함께 판단할 수
   있는 한·영 심야 이동 서비스." — 기능 채택 기준은 이 문장
6. N버스 데이터는 별도 대형 라운드 이월(최종 목표에는 포함)

## E. 확정 순서와 다음 단계

```
1. [완료] Phase 0 docs-only 정본화 커밋 (이 문서 포함 3건) — push는 별도 승인 대기
2. docs push (포그린 승인 후)
3. Phase 1A Graph Core — 포그린이 read-only 선보고 지시서 제공 예정
   (선보고 12항목 → 최종 지시서 → 구현 + 로컬 QA → 승인. 수치 QA 매트릭스는 worklog §K)
4. 별도 Robotaxi Freshness Round (8/19 공식 반영 — 1A와 코드 동시 작업 금지)
5. Phase 1B A21 vertical slice UX → 내부 연결 전체 확대 → 지도↔Graph → decision service
```

## F. 좌표

```
Git local/origin     4fdfc93 + 이번 docs-only 커밋 1건 (SHA는 커밋 보고 참조, push 미실행)
Production runtime   abb0ba7 (변동 없음)
live image           sha256:23bfedc2ba78… (변동 없음)
Round 26 / 27        CLOSED 유지 · 보존 자산(rollback/backup) 삭제 금지 유지
```

## G. 변경 0 확인 (감사 구간)

코드 0 · 데이터 0 · 서버 0 · Docker/Caddy 0 · 설치 0 · AdSense 설정 0.
외부 접근은 read-only GET만(라이브 4회 · allnosun 15회 · 서울시 공식 수 회).
미추적 보존 대상 비접촉.
