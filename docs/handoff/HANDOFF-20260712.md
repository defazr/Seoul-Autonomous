# Handoff — 2026-07-12

> 이전 핸드오프: `HANDOFF-20260630_4.md` — Round 24-B 배포 + AdSense 리뷰 요청 완료
> 이번 핸드오프: **AdSense 거절 확인 → Round 25-A audit(채팅 보고) → Round 25-C 구현·QA·커밋·푸시·라이브 배포 완료**

## 프로젝트 현재 상태 한 줄

**AdSense가 2026-07-10 "가치가 별로 없는 콘텐츠"로 거절됨. Round 25-A audit-only(채팅 보고, 파일 없음)로 원인 진단 후, 확인된 사실 오류·표시 결함만 정정하는 Round 25-C를 단독 배포 완료. 재신청 안 함. 다음은 Round 25-D(22개 상세 페이지 맥락 보강).**

---

## AdSense 상태 (2026-07-10 심사 결과)

- 승인 상태: **광고 게재가 준비되지 않은 사이트**
- 정책 위반: 발견됨 — 사유: **가치가 별로 없는 콘텐츠** (Google 게시자 네트워크 사용 기준 미충족)
- 특정 URL 지목 없음 / **ads.txt: 승인됨** / **사이트 소유권: 확인됨**
- 재신청하지 않음 — 콘텐츠 보강 라운드(25-D~) 완료 후 재신청 판단
- Round 25-A audit 결론(채팅 보고): 거절 주원인은 기술 문제가 아니라 심사자가 읽을 산문형 고유 콘텐츠 부족 + 노선 상세 22페이지의 템플릿·얇은 콘텐츠 패턴. 포그린 확정 방침: **22페이지 유지·색인 유지·noindex 금지·삭제 금지**, 표현·설명만 보강.

## Round 25-C 목적

AdSense low-value content 전체 대응이 아니라, **사용자에게 잘못된 정보를 주거나 미완성처럼 보이게 만드는 표시 결함을 먼저 정정한 소규모 정확성 라운드.** 콘텐츠 확장은 포함하지 않음(25-D 이후 별도).

## 코드·배포 좌표

- Round 25-C 커밋: **`6d11a47`** "Round 25-C: correct route facts and display issues" (7파일, +31/−16)
- 로컬 HEAD = origin/main = 라이브 서버 HEAD = **`6d11a47`**
- 새 Docker 이미지: **`c59830a89ecb`** (2026-07-12 배포)
- 직전 라이브 이미지: `fe355aa01666` (R24-B 빌드)
- 신규 롤백 태그: **`seoul-autonomous-web:rollback-86af374`** (= `fe355aa01666`) ← 직전
- 기존 롤백 태그 보존: `rollback-be8d412`(=`5b647441350e`), `rollback-c5b8abc`(=`47a89e955939`), `rollback-0a27b2c`(=`29271d937402`)

## 구현 내용 (변경 파일 7개)

`web/app/[locale]/night-bus-map/page.tsx` · `web/app/[locale]/routes/[id]/page.tsx` · `web/components/home/Hero.tsx` · `web/components/routes/RoutesList.tsx` · `web/components/ui/RouteCard.tsx` · `web/messages/ko.json` · `web/messages/en.json`

### 1. `/ko/night-bus-map` (사실 정정)

- 잘못된 "일반 시내버스 요금과 동일" 문구 제거 → **"서울 올빼미버스 요금은 카드 기준 2,500원입니다(서울시, 2026년 3월 20일 기준)."** + 탑승 전 확인 안내. 공식 출처: news.seoul.go.kr/traffic/archives/27974 (수정 2026-03-20)
- N15·N31·N61·N72·N75 노선 설명(`desc`, page.tsx 내 프로즈)에 "공식 기·종점: ○○~○○." 문장 추가 + 잘못된 기·종점 토큰만 교체:
  - N15: 도봉산→**우이동** (공식 우이동~사당역)
  - N31: 노원→**정릉동** (공식 강일동~정릉동)
  - N61: 금천→**신정동** (공식 신정동~상계동; 종점 '노원' 권역 표현은 임의 확장 안 함)
  - N72: 구파발→**수색동**, "청량리까지"→"청량리를 거쳐 **신내동**까지" (공식 수색동~신내동; 기존 경유지 합정·홍대입구·신촌·서울역·동대문·신설동·청량리 순서 전부 보존)
  - N75: 금천→**신림동** (공식 진관동~신림동)
- **지도 SVG·노선 기하·`night-bus-data.ts` 무변경.** N26·N73·A21 desc 무변경(지시 범위 외).

### 2. 단일 출발 시각 표시 (새벽A160·A741·A148·A504)

- 데이터(`firstBus === lastBus === "03:30"`)는 무변경, 표시만 정정
- `RouteCard.tsx`: firstBus===lastBus면 시각 하나만 표시 → "03:30 – 03:30" 제거
- FAQ: 신규 i18n 키 `routeDetail.aeo.a2Single` + `routes/[id]/page.tsx` 렌더 분기
  - ko: "현재 확인된 출발 시각은 {firstBus}입니다. 운행 전 최신 정보를 다시 확인하세요."
  - en: "The currently verified departure time is {firstBus}. Check the latest service information before travel."
- 심야A21 등 범위 노선은 기존 범위형 표시·FAQ 유지 (기존 `formatHours` 로직 무변경)

### 3. 버스·로보택시 개수 분리

- 합산 "12개 노선" 표현 제거
- 홈 배지(`home.hero.badge`): ko "자율주행 버스 {busCount}개 노선 · 로보택시 {robotaxiCount}개 서비스" / en "{busCount} autonomous bus routes · {robotaxiCount} robotaxi service"
- routes 필터별(`routes.countAll/countBus/countTaxi`): 전체=분리 표기, 버스 탭=버스만, 로보택시 탭=서비스만 (구 `routes.count` 키 제거)
- 개수는 기존 SSOT 계산: `getVerifiedRoutes().length`(11) / `getOnDemandServices().length`(1) / 필터 결과 length. 하드코딩 0

## 검증 결과 (전 항목 PASS)

- TypeScript `tsc --noEmit` PASS / production build PASS / 정적 생성 57/57
- 브라우저 QA(로컬 Playwright + 기설치 Chromium headless) **65항목 전부 PASS**, 스크린샷 24장
- viewport 320·390·1440 — ko/en 홈 배지 잘림·겹침·overflow·가로스크롤 없음
- routes 전체·버스·로보택시 **탭 실제 클릭** PASS (로컬 + 라이브 양쪽), 검색 적용 시 개수 문구 = 카드 수 일치
- 단일 출발 카드·FAQ ko/en PASS, 심야A21 범위 유지 PASS
- 앱 console·hydration·React 오류 0. **미승인 AdSense의 외부 광고 요청 403(googleads.g.doubleclick.net)은 앱 오류 아님** — 승인 전까지 정상 동작
- 라이브 회귀: /ads.txt 200(1줄 그대로)·AdSense 스크립트 유지·privacy/terms/robots/sitemap 200·design-preview 404·/en/night-bus-map 404·title/H1/meta/canonical/URL 무변경·루트 언어협상(307) 무변경·광고 슬롯 0
- **Caddy 완전 미접촉** (컨테이너 교체만) / newsforgreens.com 200·fazr.co.kr 200 / 컨테이너 로그 오류 0
- 포그린 독립 curl 검증으로 기준선 5개(구 요금 0·신 요금 존재·기·종점 5건·홈 배지·N72) 교차 확인 완료
- 롤백 불필요

## 유지된 금지선

- 22개 노선 상세 페이지 유지·색인 유지 — noindex·삭제·통합 없음
- `routes.json` 팩트 데이터 무변경, `_meta.publicReady` 무변경
- `night-bus-data.ts` 무변경, SVG·노선 기하 무변경
- A21 데이터·시간 무변경, N26·N73 설명 무변경
- Round 22 title·H1·URL 잠금 유지
- `/en/night-bus-map` 아직 404 (25-D 이후 신설 예정)
- 루트 언어협상 유지, alternateLinks:false 유지
- R24-A/B 산출물(ads.txt·AdSense 연결 스크립트·Privacy·Terms) 유지
- 광고 슬롯·Auto ads·CMP·Consent Mode 미구현
- **AdSense 재신청 안 함**

## 비차단 후속 관찰 (이번 라운드 수정 안 함)

- ko 320px에서 배지의 "서비스" 단어 중간 줄바꿈 (기존 CSS 기본 word-break; `keep-all` 검토는 별도 UI 라운드)
- 로보택시 탭에서 기존 새벽/심야 그룹 카드 영역 계속 노출 (기존 동작; Round 25-D/E에서 구조 확인)
- en 홈 배지는 기존 CSS `text-transform: uppercase`로 대문자 표시 (기록만)

## 운영 참고 (이번 세션에서 확인된 것)

- `.git/index.lock` 스테일 락 1회 발생 → 실행 중 git 프로세스 없음 + 0바이트 + 20분 경과 확인 후 제거. **앞으로도 반드시 같은 확인 후에만 제거.**
- `.claude/`는 저장소 .gitignore 대상이 아닙니다. 실행 환경의 전역·로컬 excludes 설정에 따라 git status --short에 ?? .claude/로 표시되거나 표시되지 않을 수 있습니다. untracked 항목 수를 고정값으로 판정하지 말고, 작업 시작 시점의 실제 git status --short 결과를 진본으로 삼아 기존 항목을 모두 보존합니다.
- 서버 untracked `Dockerfile` 보존 확인 (수동 빌드가 의존, 저장소 미커밋).

## 다음 라운드 — Round 25-D

- **기존 22개 노선 상세 페이지의 맥락·설명 보강** (노선 성격·누구에게 유용·Unknown인 이유·단일 출발의 의미·올빼미버스와의 차이·확인 기준일과 출처)
- 별도 i18n/콘텐츠 계층 사용 — `routes.json` 팩트 데이터는 유지
- noindex·삭제·통합 금지
- 절차: GPT 지시서 초안 → CoWork 이견 검토 → GPT 최종본 → Claude Code 편집 전 대조 → 구현 → 커밋·푸시·배포 각각 별도 승인
- 이후 예정: 홈/en 첫 화면 강화, /en/night-bus-map 신설, 핵심 가이드 3개(요금·결제 / 막차 상황별 / 영어 심야버스 안내) → 색인 확인 → 재신청 (2026-06-30 계획 + 2026-07-12 포그린 확정 방침 기준)
