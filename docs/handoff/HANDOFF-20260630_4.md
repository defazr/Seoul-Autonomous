# Handoff — 2026-06-30 (4차)

> 이전 핸드오프: `HANDOFF-20260630_3.md` — Round 24-A 배포 완료
> 이번 핸드오프: **Round 24-B(ads.txt + AdSense 연결 스크립트) 구현 → 커밋 → 푸시 → 라이브 배포 완료 + AdSense 사이트 소유권 확인·리뷰 요청 완료**

## 프로젝트 현재 상태 한 줄

**Round 24-B 단독 배포 완료. AdSense 사이트 소유권 확인·리뷰 요청 완료. 현재 "사이트의 광고 게재 가능 여부 검토 중"(심사 결과 미확정). 심사 결과 전까지 사이트 흔들지 않음 — 광고 슬롯·Auto ads·CMP·Consent Mode 미구현.**

---

## Round 24-B 목적

AdSense 사이트 연결·심사 요청을 위한 최소 코드(연결 스크립트 + ads.txt) 추가. 실제 광고는 게재하지 않음(슬롯·Auto ads 미구현).

## 커밋 / 배포

- 구현 커밋: **`86af374`** "Round 24-B: add ads.txt and AdSense connection script" (2개 파일, +6)
- 라이브 배포일: **2026-06-30**
- origin/main · 로컬 HEAD · 서버 라이브 HEAD 모두 **`86af374`**
- 새 Docker 이미지 ID: **`fe355aa01666`** (manifest `sha256:fe355aa…`)
- 롤백 태그:
  - **`seoul-autonomous-web:rollback-be8d412`** (= `5b647441350e`, R24-A 빌드) ← 직전
  - `rollback-c5b8abc` (= `47a89e955939`)
  - `rollback-0a27b2c` (= `29271d937402`)

## 변경 내용 (2개 파일)

- `web/public/ads.txt` (신규) — 정확히 1줄: `google.com, pub-7976139023602789, DIRECT, f08c47fec0942fa0`. UTF-8·BOM 없음·LF·끝 개행 1개·후행 공백 없음. publisher ID 형식은 **`pub-…`**(스크립트의 `ca-pub-…`와 구분).
- `web/app/[locale]/layout.tsx` — `</body>` 직후에 raw `<script async src="…adsbygoogle.js?client=ca-pub-7976139023602789" crossOrigin="anonymous" />` 1요소 추가. React 19 리소스 hoisting으로 SSR 시 `<head>`로 hoist.

## 검증 결과 (전 항목 PASS)

- **빌드**: `npm run build` PASS (Next 16.2.4, TypeScript 통과, 57/57 정적 생성, 에러 0)
- **라이브 `/ads.txt`**: 200, `text/plain; charset=UTF-8`, **redirect 없음**, 내용 정확(1줄, 끝 개행 1개)
- **연결 스크립트**: `/ko·en`, `/ko·en/privacy`, `/ko·en/terms`, `/ko/night-bus-map` 초기 HTML 기준 **실제 `<script>` 태그 1개/페이지, 전부 `<head>` 내부**, `async`·`crossorigin="anonymous"`·client 값 정확. (RSC/Flight 페이로드의 URL 문자열은 실태그 아님 — 비계수)
- **제외**: `/design-preview` 하드 404 유지, 임의 404에 실 adsense 태그 0, `<ins class="adsbygoogle">` 0건, 실제 광고 UI 0
- **무변경**: Privacy·Terms·robots·sitemap·Round 22(지도 title/H1/meta)·GA4(`G-ND7JGQ62QX`) 그대로
- **회귀**: `/`(언어협상)·`/ko`·`/en`·`/ko/night-bus-map`·privacy·terms·robots·sitemap 모두 200
- **서버 로그**: error·exception·hydration·warn 0건
- **Caddy 완전 미접촉** (컨테이너 교체만)
- **다른 사이트 헬스체크**: newsforgreens.com 200, fazr.co.kr 200

## AdSense 계정 상태 (2026-06-30 기준)

- `seoulautonomous.com` 사이트 추가 **완료**
- 사이트 **소유권 확인 완료**
- 연결 방식: **AdSense 코드 스니펫** 방식 선택
- **리뷰 요청 완료**
- 현재 상태: **"사이트의 광고 게재 가능 여부 검토 중"**
- 승인 여부는 **아직 미확정** (승인 보장 아님)
- 심사 결과 전까지 광고 슬롯·Auto ads·CMP·Consent Mode **구현 금지**

## ⚠ 운영 주의사항 (반드시 기억)

- **서버 저장소에 추적 밖 `Dockerfile`(untracked) 존재** — 수동 Docker 빌드가 의존. `git pull --ff-only`는 tracked만 갱신. 저장소엔 Dockerfile 미커밋.
- **롤백 절차**: 배포는 `:latest`를 덮음 → 다음 배포 전 현재 이미지를 `rollback-<해시>`로 박제. 직전 = `rollback-be8d412`.
- 로컬 untracked 3건(`docs/handoff/HANDOFF-20260608.md`, `round19-final-이식지시서.md`, `route/`)은 계속 미변경·커밋 제외.

## 다음 작업 — 심사 결과에 따라 분기 (현재 대기)

- **A. 승인 시**: Auto ads 사용 여부 결정 → 광고 노출 범위 설계 → CMP·Consent Mode 검토 → 지도·검색·버튼 주변 광고 제외 → 승인 후 별도 R24-C/D 라운드.
- **B. 거절 시**: 거절 사유 원문 확인(추정 수정 금지) → 사유별 audit 후 별도 라운드.
- **C. 심사 중(현재)**: 코드·ads.txt·Privacy·Terms·title·H1·URL 구조 **수정 금지**. 불필요한 재신청·사이트 삭제·코드 제거 금지. **사이트를 흔들지 않는 것이 최우선.**
