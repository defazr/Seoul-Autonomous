# Handoff — 2026-06-30

> 이전 핸드오프: `HANDOFF-20260628_2.md` — Privacy 지시서·audit·구현 방안 확정
> 이번 핸드오프: **Round 22-Privacy 본 작업 구현 → 커밋 → 푸시 → 라이브 배포 완료**

## 프로젝트 현재 상태 한 줄

**Round 22-Privacy(/ko/privacy + /en/privacy 정정) 단독 배포 완료. 라이브 §9 회귀검증 전 항목 PASS. 다음은 Round 22(지도/업데이트) 지시서 대기.**

---

## 이번 세션 완료 작업

### 1. 새 협업 구조 확정
- **GPT**: 지시서 작성·최종 판단
- **Claude Code**: 구현자 관점에서 충돌·최소 대안 제시 (무조건 동의 X, 실제 코드 기준 검토)
- **Claude UI**: 핸드오프 공유·문서 정리
- 최종 선택은 포그린이 잠금 문서·전체 범위 기준으로 결정

### 2. Privacy 본 작업 구현 (커밋 `0a27b2c`)
변경 파일 7개:
| 파일 | 변경 |
|------|------|
| `web/lib/types/legal.ts` | `orderedList?`, `paragraphsAfter?` (LegalSection), `developerLabel?` (LegalContact) 선택 필드 추가 |
| `web/components/legal/LegalDocument.tsx` | `<ol>` 렌더 + `paragraphsAfter` 렌더 + 운영자 라벨 분기 |
| `web/components/legal/LegalDocument.module.css` | `.orderedList`/`.orderedItem` 추가 |
| `web/data/legal/privacy.ko.ts` | 8→14개 섹션 전면 교체, contact 교체, 날짜 2026-06-30 |
| `web/data/legal/privacy.en.ts` | 동일 |
| `web/messages/ko.json` | privacyTitle·Description 교체 (dash 제거) |
| `web/messages/en.json` | 동일 |

### 3. 구현 중 구현자가 잡은 구조 이슈 (전부 해결)
- **`paragraphsAfter` 필드 신설**: 리스트 *뒤* 마무리 문장이 있는 섹션 5곳(§4·6·7·8·10). 기존 구조엔 자리가 없어 그냥 넣으면 문장이 리스트 위로 올라감. 선택 필드 1개로 순서 보존(Terms 미사용 → 영향 0).
- **§14 연락처 중복 충돌**: A안 채택 — §14 본문은 안내문 1줄, 운영자·이메일·소재지는 contactBlock 1회만. `developerLabel?` 선택 필드로 Privacy만 `운영자:`/`Operator:` 라벨 표시(Terms 미사용).
- **백틱 처리**: 렌더러가 백틱 미처리 → `_ga`·`_ga_*`·측정ID·`seoulautonomous.com` 백틱 제거하고 일반 텍스트.

### 4. GPT 최종 태클 4건 반영 (스크린샷 검수 후)
1. §3 측정 ID 문장 삭제 (ko·en)
2. §3 내부 기술상태 삭제 (GTM 미설치 / Ads·AdSense 미연결 / Consent·배너 미구현)
3. §7 광고 미사용 단정 문장 삭제
4. §10 정보주체 권리(열람·정정·삭제·처리정지 + 식별 한계) 문단 추가
- 결과: §3 한·영 비대칭 허용(KO 2문단·EN 1문단, 의미 동등), 운영자 `Seoul Autonomous` 유지

### 5. 배포
- 커밋 `0a27b2c` (author `seokyoung oh <thisiz43@gmail.com>` 정정) → `git push origin main` → 서버 git pull → docker build/run
- **Caddy 미변경.** 기존 사이트 헬스체크 통과(newsforgreens.com 200, fazr.co.kr 200)

---

## 라이브 §9 회귀검증 (전 항목 PASS)

- `/ko/privacy`·`/en/privacy` 200
- title: `개인정보처리방침 Seoul Autonomous` / `Privacy Policy Seoul Autonomous` (dash 제거)
- H1: `개인정보처리방침` / `Privacy Policy`
- 14개 섹션, §4 순번 목록(`<ol>`) 정상
- 시행일=최종수정일 `2026-06-30`
- §3 측정ID·GTM·AdSense·Consent 본문 잔존 0
- §10 정보주체 권리 문단 렌더
- 운영자 1회 노출 / `seoulautonomous@protonmail.com` / `fazr` 잔존 0
- GA4 gtag 로드(`?id=G-ND7JGQ62QX`) 유지 (본문 분리)
- canonical self / html lang / noindex 없음 / HTTP Link hreflang 미발생
- sitemap `/ko/privacy`·`/en/privacy` 등록 유지 / robots 200
- 타 페이지 회귀 전부 200

---

## 다음 세션 재개 지점

**Round 22 본작업(지도/업데이트) 지시서 대기.**

- 잠금 문서 §1: Privacy 배포 후 Round 22 시작까지 몇 시간~다음 날 간격
- 참조: `docs/worklogs/ROUND-22-FINAL-PLAN.md`, `docs/worklogs/ROUND-22-PRIVACY-INSTRUCTIONS.md`
- Round 22 핵심: 지도 페이지 title/meta/DOM, 업데이트 글, 내부 링크, JSON-LD(WebPage+BreadcrumbList), DOM 순서 변경
- 이후: Round 23(GA4 이벤트), Round 24(AdSense)

## 보류 항목 (정직성 유지 — "법적 완전 충족" 표현 금지)
- 운영자 실명·법적 주체 명칭 확인 → 별도 법률 점검 라운드
- 국외이전 정식 고지(이전받는 자·항목·시기·거부방법 등) → 별도 법률 점검 라운드
- 기타: /ko/updates title suffix 누락, 검색 8개 제한, 크롬 다크톤, 영문 노선도 등 (메모리 보류 목록)

## 미추적 파일 (계속 보관 — 건드리지 않음)
- `docs/handoff/HANDOFF-20260608.md` (untracked)
- `round19-final-이식지시서.md` (untracked)
- `route/` (untracked)
