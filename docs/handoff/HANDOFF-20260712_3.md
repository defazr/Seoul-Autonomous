# Handoff — 2026-07-12 (3차)

> 이전 핸드오프: `HANDOFF-20260712_2.md` — Round 25-D 배포 완료
> 이번 핸드오프: **Round 25-E(홈 허브 보강 + /en/night-bus-map 영문 가이드) 구현 → 검증 → 커밋 → 푸시 → 라이브 배포 완료**

## Round 25-E 목적

1. ko/en 홈에 사이트 가치와 주요 콘텐츠 경로를 설명하는 허브 섹션 추가
2. 지금까지 404였던 `/en/night-bus-map`을 **신규 영문 텍스트 가이드**로 공개 (사이트 첫 영문 심야버스 안내 — en 쪽 콘텐츠 공백 해소)

## 최종 좌표

- Round 25-E 코드 커밋: **`5fd776f`** "Round 25-E: add English night bus guide and home hub" (11파일, +508/−5)
- local = origin = live server: **`5fd776f`**
- live Docker image: **`a8f2cc4e6ae9`** (2026-07-12 배포)
- 직전 라이브 이미지: `49782f5a17ec` (R25-D 빌드)
- 신규 롤백 태그: **`rollback-c1781a1`** (= `49782f5a17ec`) ← 직전
- 기존 롤백 태그 보존: `rollback-6d11a47`, `rollback-86af374`, `rollback-be8d412`, `rollback-c5b8abc`, `rollback-0a27b2c`
- 롤백 불필요 · Caddy 미접촉 · **AdSense 재신청 안 함**

## 영문 가이드 `/en/night-bus-map`

- 콘텐츠: H1 "Seoul Night Bus (Owl Bus) Guide" + Hero 요약 + 기준일(2026-03-20, 서울시) + Quick facts(**14노선 / 140대 / 23:00–06:00 / 카드요금 KRW 2,500**) + 설명 H2 3개 + **14개 노선 전체(기점↔종점, 누락·중복 0)** + 한국어 인터랙티브 지도 CTA(`/ko/night-bus-map`, "지도는 한국어" 안내 포함) + Before you ride
- SEO: **self-canonical**, index/follow, **hreflang·x-default 없음**(ko 지도와 의도적으로 미연결 — `buildPageMetadata`의 자동 hreflang을 피해 자체 metadata 객체 사용), JSON-LD 없음, **sitemap 독립 엔트리**(monthly·0.7, alternates 없음)
- 구조: 콘텐츠는 `web/data/night-bus-guide/night-bus-guide.en.ts` TS 모듈(`as const` + 14개 컴파일타임 길이 가드), 렌더는 `web/components/night-bus-guide/NightBusGuideEn.tsx`
- 금지 유지: 현금·청소년 요금, 환승 할인 세부, K-Pass, 노선별 첫차/막차/배차, A21, 운영사·예약, real-time/official/complete 표현 — 전부 미포함
- **최종 영문 교정 3건 반영** (포그린 검수): inspect→check ×2, "in…travelling"→"on…traveling"

## 홈 (ko/en)

- **가치 섹션 신규**: FeaturedRoutes 다음 위치. ko "이 사이트에서 확인할 수 있는 것" / en "What you can find here" + 각 3개 링크 카드(노선 상세·심야버스 노선도/Night bus guide·업데이트) — locale별 자동 라우팅(`/ko/*`, `/en/*`), 숫자 하드코딩 없음
- **ko mapPromo**: 기존 문구("서울 심야버스 노선과 환승 노선도 보기")·링크·시각 결과 완전 유지 — 하드코딩 문구를 i18n 키로만 이동
- **en mapPromo 신규**: "Seoul Night Bus guide" + 설명 + "View the English guide" → **`/en/night-bus-map`** (ko 지도 직결 아님, 영문 지도로 오해시키는 문구 없음)
- 기존 H1·Hero·FeaturedRoutes·CTA·Footer 무변경

## 변경 파일 (11개, +508/−5)

신규 5: `web/data/night-bus-guide/night-bus-guide.en.ts` · `web/components/night-bus-guide/NightBusGuideEn.tsx`+`.module.css` · `web/components/home/ValueSection.tsx`+`.module.css`
수정 6: `night-bus-map/page.tsx`(staticParams en·en metadata·en 렌더 분기) · 홈 `page.tsx` · 홈 `page.module.css` · `sitemap.ts` · `ko.json` · `en.json`

## 검증·회귀 (전 항목 PASS)

- typecheck·production build PASS, **정적 생성 58/58** (57→58, en 가이드 +1)
- **ko night-bus-map 무회귀**: 로컬 빌드 vs 라이브 비교 — title·canonical·hreflang(0)·가시 본문 4,146자 완전 동일. 본문·메타 무변경
- Round 25-C 유지(2,500원·기·종점 5건·03:30 단일·11+1 배지) · Round 25-D 유지(22페이지 노선 안내)
- 브라우저 QA: `/ko` `/en` `/ko/night-bus-map` `/en/night-bus-map` × 320/390/1440 = **12조합 PASS**, pageerror 0
- ads.txt·Privacy·Terms·robots·sitemap 200 · **광고 슬롯 0** · design-preview 404 · **루트 언어협상 307 유지** · 컨테이너 로그 오류 0 · newsforgreens/fazr 200
- 기존 untracked 항목과 `.claude/` 보존 원칙 유지 (실제 `git status --short` 진본, 고정 개수 판정 금지)

## 다음 작업

- **Round 25-F**: 한국어 실용 가이드 2개 — ① 요금·결제·환승 ② 막차 이후 이동 시나리오 (GPT 초안 → CoWork 이견 → GPT 최종본 절차)
- **Round 25-G**: 최종 라이브 audit → 색인 상태 확인 → 통과 후 포그린이 AdSense 수동 재신청
